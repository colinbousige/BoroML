# Python runtime environment for NAPS helper scripts.
# Adapt to your HPC stack (modules, conda, venv, etc.).

# Example with module + conda
module purge
module load python/3.11

# If you use conda, uncomment and edit:
# source /path/to/miniconda3/etc/profile.d/conda.sh
# conda activate naps

# Optional diagnostics
python --version
